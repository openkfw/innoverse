{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "publish": "npm run build && func azure functionapp publish fa-innobuddy"
  },
  "devDependencies": {
    "@azure/functions": "^2.0.0",
    "@types/node": "14.x",
    "typescript": "^4.9.5"
  }
}
