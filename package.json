{
  "devDependencies": {
    "@commitlint/cli": "^19.4.1",
    "@commitlint/config-conventional": "^19.4.1",
    "commit-and-tag-version": "^12.4.2",
    "commitlint": "^19.4.1",
    "husky": "^9.1.5"
  },
  "scripts": {
    "prepare": "husky",
    "release": "commit-and-tag-version --skip.commit --skip.tag --no-verify",
    "commitlint": "commitlint --edit"
  }
}
